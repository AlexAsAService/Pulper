# Environment — Local Overrides
# Copy to .env and fill in values. Never commit .env.
#
# Currently unused — placeholder for future config surface.
# (e.g., registry credentials for local CD testing, log level, etc.)

# IMAGE_TAG=minimal
# LOG_LEVEL=info

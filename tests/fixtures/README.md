# fixtures/

Place representative sample files here for smoke testing.

At minimum, add a `sample.pdf` (or other supported format) before running `scripts/smoke.sh`.

Files committed here should be small, non-sensitive, and publicly redistributable.
